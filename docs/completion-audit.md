# 交付验收状态

当前状态：软件已交付，完整目标尚未验收；等待外部接入资料。

## 已有证据

- ImageGen 原型：outputs/prototypes 中三张图；用户选择第 3 张，项目 docs/design-reference.png 保留参考。
- Vue + FastAPI：运行入口 5176 与 8016；源码包含前后端、SQLite、启动脚本与依赖清单。
- 知识图谱：交互关系、原因/措施/引用、本地知识检索、收藏、经验与探究记录。视觉对照及浏览器记录见 design-qa.md。
- MQTT：管理员配置、真实 TCP MQTT 协议、遥测校验、指令与匹配回执、超时策略。本机 Broker 测试通过。
- 识别统计与日志追溯：实际上传流程、数量/分布/平均置信度留存、设备归属和证据快照已验证。
- 全部软件模块及其证据范围见 requirements-coverage.md。最新 tests/test_api.py 与 tests/test_mqtt_live.py 合计 13 项通过；生产构建通过。

## 当前阻塞的直接观测

本次访问实际运行的 FastAPI：

- MQTT connected=false，host_configured=false。
- 以已有照片请求 demo=false 的识别：HTTP 409，提示未配置 YOLO_MODEL_PATH。
- 问答已升级为 mode=neo4j_graph，真实 Neo4j 持久化、Cypher 检索和关系遍历；停库 503，不回退。未接入生成模型，不宣称生成式 RAG。

不能以模拟检测框或本机 Broker 回执证明真实模型准确率与物理执行器动作。需要用户提供训练权重路径、类别含义、MQTT Broker 地址/端口/主题及可联调设备；密码由用户在配置页填写。

## 其他尚未证明的范围

- 手机端为响应式 Vue 网页；未交付原生 APP 安装包。
- 身份为本机教学角色，未证明多学校、多学生生产账户隔离。
- 相机和麦克风实际硬件能力尚未验收。
- 视觉复刻采用可交互组件，保留已记录的 P3 图标、节点坐标与字形差异，不宣称逐像素一致。

这些边界保留在交付说明中，不能据此把完整目标标记完成。收到实际接入资料与目标运行环境后继续验收。
